name: badminton
description: "A new Flutter project for Badminton Empire Management."

# 제국의 보안을 위해 퍼블리싱을 막아두었나이다.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- 👑 제국의 외부 지원 도구들 ---
  cupertino_icons: ^1.0.6
  firebase_core: ^2.24.2
  cloud_firestore: ^4.14.0

  # 인스타그램 및 외부 웹사이트 연결을 위한 마법
  url_launcher: ^6.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # 린트 설정을 통해 코드의 기강을 잡나이다.
  flutter_lints: ^3.0.0

  # --- 👑 앱 아이콘 자동 생성을 위한 도구 ---
  flutter_launcher_icons: ^0.13.1

# --- 🎨 앱 아이콘 설정 (이미지 파일 준비 후 활성화 하소서) ---
# 아래 주석을 풀고 assets/icon/logo.png 경로에 이미지를 두면
# 명령어 한 줄로 앱 아이콘을 바꿀 수 있나이다.
# flutter_launcher_icons:
#   android: "launcher_icon"
#   ios: true
#   image_path: "assets/icon/logo.png"
#   min_sdk_android: 21

flutter:
  uses-material-design: true

  # --- 🖼️ 이미지 자원 관리 ---
  # 앱 아이콘이나 배경 이미지를 쓸 때 아래 주석을 풀고 경로를 등록하소서.
  # assets:
  #   - assets/icon/